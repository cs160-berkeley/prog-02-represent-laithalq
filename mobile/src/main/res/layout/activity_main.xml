<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.cs160.joleary.represent.MainActivity">

    <TextView
        android:id="@+id/find_reps_label"
        android:layout_width="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_above="@+id/input_zipcode"
        android:layout_marginBottom="20dp"
        android:layout_height="wrap_content"
        android:hint="@string/find_your_reps_label"
        android:textSize="25dp"
        android:textColor="@android:color/primary_text_dark"
        android:textAlignment="center"/>
    <EditText
        android:id="@+id/input_zipcode"
        android:layout_width="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_above="@+id/zipcode_btn"
        android:layout_marginBottom="20dp"
        android:layout_height="wrap_content"
        android:hint="@string/hint_zipcode"
        android:textAlignment="center"
        android:inputType="number"/>

    <Button
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:text="@string/zipcode_button_text"
        android:id="@+id/zipcode_btn"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:padding="5dp"
        android:background="@android:color/holo_blue_dark"
        android:textColor="@android:color/white"/>
    <Button
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:text="@string/location_button_text"
        android:id="@+id/location_btn"
        android:layout_centerHorizontal="true"
        android:layout_below="@id/zipcode_btn"
        android:layout_marginTop="10dp"
        android:padding="5dp"
        android:background="@android:color/holo_green_dark"
        android:textColor="@android:color/white"/>

</RelativeLayout>
